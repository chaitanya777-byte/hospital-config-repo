tyspring:
  datasource:
    url: jdbc:mysql://localhost:3306/hospital_db
    username: root
    password: SecuritY@1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
